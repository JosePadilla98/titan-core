---
import Contact from '@components/forms/Contact.astro';
import { siteConfig } from '@data/config';
import type { PaddingSize, ThemeColor } from '@utils/styleUtils';
import { COOL_TEXT_STYLE, getBackgroundColor, getPaddingClass, getTextColor } from '@utils/styleUtils';
import { Mail, MapPin, Phone } from 'lucide-astro';
export interface Props {
    content?: {
        title?: string;
        description?: string;
        contactInfo?: {
            email?: string;
            phone?: string;
            address?: string;
        };
    };
    background?: ThemeColor;
    padding?: PaddingSize;
    paddingTop?: PaddingSize;
    paddingBottom?: PaddingSize;
}
const { content, background = 'base', padding, paddingTop, paddingBottom } = Astro.props;

const bgColor = getBackgroundColor(background);
const textColor = getTextColor(background);
const paddingClass = getPaddingClass({ padding, paddingTop, paddingBottom });
---

<section class:list={[bgColor, paddingClass]}>
    <div class="container mx-auto px-4 space-y-24 lg:space-y-32 overflow-hidden">
        <!-- Contact Information Section -->
        <section data-aos="fade-up" data-aos-delay="200" class="max-w-4xl mx-auto text-center relative">
            <div
                class="absolute inset-0 bg-gradient-to-r from-primary/5 to-secondary/5 rounded-3xl -z-10 lg:transform lg:rotate-1"
            >
            </div>
            <div class="px-8 py-12">
                <h2 class="title-color mb-8 font-bold">Información de contacto</h2>
                <p class:list={[textColor, 'text-lg leading-relaxed']}>
                    Estamos aquí para ayudarle con sus preguntas e inquietudes. No dude en contactarnos.
                </p>
                <div class:list={['flex flex-col gap-4 mt-8', textColor]}>
                    <div
                        class:list={[
                            'flex items-center gap-4 p-4 rounded-xl bg-white/5 hover:bg-white/10 transition-colors duration-200',
                        ]}
                        data-aos="fade-up"
                        data-aos-delay="300"
                    >
                        <div
                            class="flex items-center justify-center w-12 h-12 rounded-full bg-gradient-to-r from-blue-500 to-purple-600 text-white"
                        >
                            <Mail size={20} aria-hidden="true" />
                        </div>
                        <div class="text-left">
                            <div class="font-semibold">Correo electrónico</div>
                            <a
                                href={`mailto:${siteConfig.Socials.Email}`}
                                class="hover:text-blue-400 transition-colors duration-200">{siteConfig.Socials.Email}</a
                            >
                        </div>
                    </div>
                    <div
                        class:list={[
                            'flex items-center gap-4 p-4 rounded-xl bg-white/5 hover:bg-white/10 transition-colors duration-200',
                        ]}
                        data-aos="fade-up"
                        data-aos-delay="400"
                    >
                        <div
                            class="flex items-center justify-center w-12 h-12 rounded-full bg-gradient-to-r from-green-500 to-teal-600 text-white"
                        >
                            <Phone size={20} aria-hidden="true" />
                        </div>
                        <div class="text-left">
                            <div class="font-semibold">Teléfono</div>
                            <a
                                href={`tel:${siteConfig.Socials.Phone}`}
                                class="hover:text-green-400 transition-colors duration-200"
                                >{siteConfig.Socials.Phone}</a
                            >
                        </div>
                    </div>
                    <div
                        class:list={[
                            'flex items-center gap-4 p-4 rounded-xl bg-white/5 hover:bg-white/10 transition-colors duration-200',
                        ]}
                        data-aos="fade-up"
                        data-aos-delay="500"
                    >
                        <div
                            class="flex items-center justify-center w-12 h-12 rounded-full bg-gradient-to-r from-orange-500 to-red-600 text-white"
                        >
                            <MapPin size={20} aria-hidden="true" />
                        </div>
                        <div class="text-left">
                            <div class="font-semibold">Ubicación</div>
                            <a
                                href={`https://maps.google.com/?q=${siteConfig.Socials.Location}`}
                                class="hover:text-orange-400 transition-colors duration-200"
                                >{siteConfig.Socials.Location}</a
                            >
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- Contact Form Section -->
        <section data-aos="fade-up" data-aos-delay="400" class="max-w-4xl mx-auto text-center relative">
            <div
                class="absolute inset-0 bg-gradient-to-l from-primary/5 to-secondary/5 rounded-3xl -z-10 lg:transform lg:-rotate-1"
            >
            </div>
            <div class="px-8 py-12">
                <h2 class="title-color mb-8 font-bold">Envíenos un mensaje</h2>
                <p class:list={[textColor, 'text-lg leading-relaxed mb-8']}>
                    Complete el formulario y nos pondremos en contacto con usted lo antes posible.
                </p>
                <div class="form-container" data-aos="fade-up" data-aos-delay="500">
                    <script src="https://js-eu1.hsforms.net/forms/embed/147273914.js" defer></script>
                    <div
                        class="hs-form-frame"
                        data-region="eu1"
                        data-form-id="ba27f7cc-8271-4f29-bd9b-a0993919cc09"
                        data-portal-id="147273914"
                    >
                    </div>
                </div>
            </div>
        </section>
    </div>
</section>

<style>
    .title-color {
        font-size: var(--text-h3);
        background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-secondary) 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        font-weight: 700;
    }
</style>
